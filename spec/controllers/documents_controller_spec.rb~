require 'spec_helper'

describe DocumentsController do
  describe 'create a new document' do
    it 'should throw an error if user doesnt have permission (sad path)' do
      


      @fake_user = mock('User', :id => '1')
      User.should_receive(:find_or_create_from_auth_hash).and_return(@fake_user)
      
      post :create, {:provider => 'developer'}
    end

    it 'should create a document (happy path)' do
      
    end
  end

  describe 'destroy a document' do
    it 'should throw an error if user doesnt have permission (sad path)' do
    end

    it 'should destroy the document (happy path)' do
    end
  end
end
